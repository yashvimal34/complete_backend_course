// Arrow Functions

const toProperCase = (name) => {
    return name.charAt(0).toUpperCase() + name.slice(1).toLowerCase();
}
console.log(toProperCase("yAsH"))
console.log(toProperCase("saRTHak"))

// This is about functions that is reusable and when we need them we can call anytime to use them.