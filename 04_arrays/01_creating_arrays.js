// Array example
const fruits = ['apple', 'banana', 'mango'];

// 2. Creating Arrays

// Array Literal (Recomended)
const numbers1 = [10, 20, 30];

// Array Contructer (Rarely needed)
const numbers2 = new Array[40, 50, 60];

// ⚠️ Pitfall:
new Array(5); // creates empty array with length 5